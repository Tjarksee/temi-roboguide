<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ich kann den nächsten Ort nicht erreichen"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_gravity="center_horizontal" />

    <TextView
        android:id="@+id/message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ich kann den nächsten Ort nicht erreichen was soll ich machen?"
        android:layout_marginTop="8dp"
        android:layout_gravity="center_horizontal" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <Button
            android:id="@+id/btn_retry"
            android:layout_width="191dp"
            android:layout_height="92dp"
            android:layout_gravity="center_horizontal"
            android:backgroundTint="@color/pauseButton"
            android:text="Erneut versuchen" />

        <Button
            android:id="@+id/btn_skip"
            android:layout_width="191dp"
            android:layout_height="92dp"
            android:layout_gravity="center_horizontal"
            android:backgroundTint="@color/pauseButton"
            android:text="Überspringen" />

        <Button
            android:id="@+id/btn_cancel"
            android:layout_width="191dp"
            android:layout_height="92dp"
            android:layout_gravity="center_horizontal"
            android:backgroundTint="@color/pauseButton"
            android:text="Abbrechen" />
    </LinearLayout>

</LinearLayout>
